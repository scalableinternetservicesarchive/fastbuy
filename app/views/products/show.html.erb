<!--p id="notice"><%= notice %></p-->

<div id='title'>
  <strong>Title:</strong>
  <%= @product.title %>
</title>

<div id='description'>
  <strong>Description:</strong>
  <%= sanitize(@product.description) %>
</div>

<div>
  <%= image_tag(@product.image_url) %>
 
  <span id='product_button'><%= link_to 'Add to Cart', line_items_path(product_id: @product, quantity: 1), remote: true, method: :post, class: "btn"  %></span>
</div>

<div id='product_price'>
  <strong>Price:</strong>
  <%= @product.price %>
</div>

<div id='product_rating'>
  <strong>Rating:</strong>
  <%= @product.rating %>
</div>

<div id= 'product_quantity'>
  <strong>Quantity:</strong>
  <%= @product.quantity %>
</div>

<!--%= button_to 'Add to Cart', line_items_path(product_id: @product, quantity: 1), remote: true  %-->


